// A utility function to prevent "unused variable" TypeScript errors
// while keeping the imports for future use without affecting runtime
export function keepForLater<T>(_value: T): void {
  // This function does nothing at runtime
} 